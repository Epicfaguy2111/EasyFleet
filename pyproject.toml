[project]
name = "easyfleet"
version = "0.1.0"
description = "EasyFleet Backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg",
    "fastapi",
    "google-genai>=2.19.0",
    "pydantic",
    "sqlalchemy",
    "uvicorn",
]

[tool.uv]
package = false
