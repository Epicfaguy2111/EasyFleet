[project]
name = "easyfleet"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Epicfatguy", email = "geldenhuys120@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi>=0.141.1",
    "pydantic>=2.13.4",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.4",
]

[project.scripts]
easyfleet = "easyfleet:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
