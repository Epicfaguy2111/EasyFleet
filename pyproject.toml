[project]
name = "easyfleet"
version = "0.1.0"
description = "EasyFleet Backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg",
    "fastapi",
    "pydantic",
    "sqlalchemy",
    "uvicorn",
]

[tool.uv]
package = false